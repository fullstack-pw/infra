apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sandboxy-apps
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/fullstack-pw/infra.git
    targetRevision: HEAD
    path: argocd-apps/app-of-apps/sandboxy

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
