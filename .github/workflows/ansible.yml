name: Ansible Provisioning

on:
  push:
    branches: [main]

permissions: {}

jobs:
  ansible:
    uses: homelabz-eu/pipelines/.github/workflows/ansible.yml@main
    secrets: inherit # pragma: allowlist secret
